'use client';
import { useState } from 'react';
import { BookOpen, ChevronRight, X } from 'lucide-react';
export default function AboutProject() {
 const [open, setOpen] = useState(false);
 return <><button type="button" onClick={() => setOpen(true)} className="mecha-button inline-flex items-center gap-2 border border-pink-300 bg-white/70 px-4 py-2 text-xs font-bold uppercase tracking-[0.16em] text-pink-600 transition hover:border-pink-500 hover:bg-pink-50 hover:shadow-[0_0_18px_rgba(236,72,153,0.24)]"><BookOpen size={15} />Project briefing</button>{open && <div className="fixed inset-0 z-50 grid place-items-center bg-slate-900/20 p-5 backdrop-blur-sm" role="dialog" aria-modal="true" aria-labelledby="about-title"><section className="mecha-panel relative w-full max-w-2xl overflow-hidden border border-pink-300 bg-white/80 p-7 shadow-[0_20px_70px_rgba(190,24,93,0.2)] backdrop-blur-xl sm:p-10"><button type="button" onClick={() => setOpen(false)} className="absolute right-5 top-5 text-slate-400 hover:text-pink-600" aria-label="Close project briefing"><X size={20}/></button><p className="font-mono text-[10px] font-bold uppercase tracking-[0.28em] text-pink-500">Life RPG // operating concept</p><h2 id="about-title" className="mt-3 max-w-lg text-3xl font-black uppercase tracking-tight text-slate-800 sm:text-4xl">Your day is the next mission.</h2><p className="mt-6 max-w-xl leading-7 text-slate-600">Life RPG converts real-world routines into clear, motivating directives. Set a personal objective or request a generated mission, then build momentum by completing the work in front of you.</p><div className="mt-8 grid gap-3 sm:grid-cols-3">{[['01','Create','Turn any task into a directive.'],['02','Execute','Focus on the mission, not the noise.'],['03','Level up','Stack completions and advance.']].map(([number,title,text]) => <div key={number} className="border border-slate-200 bg-white/60 p-4"><span className="font-mono text-xs font-bold text-pink-500">{number}</span><h3 className="mt-2 font-bold uppercase tracking-wide text-slate-800">{title}</h3><p className="mt-1 text-xs leading-5 text-slate-500">{text}</p></div>)}</div><button type="button" onClick={() => setOpen(false)} className="mecha-button mt-8 inline-flex items-center gap-2 bg-pink-500 px-5 py-3 text-xs font-bold uppercase tracking-[0.16em] text-white shadow-[0_0_20px_rgba(236,72,153,0.35)] hover:bg-pink-600">Enter command deck <ChevronRight size={15}/></button></section></div>}</>;
}
